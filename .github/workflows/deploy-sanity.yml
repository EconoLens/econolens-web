name: Deploy Sanity Studio
on:
  push:
    branches:
      - main
    paths:
      - 'sanity/**'
      - 'sanity.config.ts'
      - 'package.json'
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy to Sanity Hosting
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Deploy Sanity Studio
        run: |
          echo "Deploying studio to econolens.sanity.studio..."
          ./node_modules/.bin/sanity deploy econolens --yes
          echo "Deploy command exited with code $?"
        env:
          SANITY_AUTH_TOKEN: ${{ secrets.SANITY_AUTH_TOKEN }}
